IN_DIR = './sample_classify'
OUT_DIR = './output'
IMG_TYPE = 'jpg'
PERCENTAGE = '70'

select:
	python select.py --in_dir $(IN_DIR) --out_dir $(OUT_DIR) --img_type $(IMG_TYPE) --percentage $(PERCENTAGE)
