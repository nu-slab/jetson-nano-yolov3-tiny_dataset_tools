IN_DIR = './sample'
OUT_DIR = './output'
IMG_TYPE = 'jpg'
RENAME_STR = '2022_05_17_'

rename:
	python rename.py --in_dir $(IN_DIR) --out_dir $(OUT_DIR) --img_type $(IMG_TYPE) --rename_str $(RENAME_STR)
